extends:
  - recommended
rules:
  info-license: 'off'
  no-required-schema-properties-undefined: 'off'
ignore:
  - '**/*.md'
  - openapi/index.json
preprocessors:
  remove-x-internal:
    remove:
      - x-topics
      - x-tags
      - x-stoplight
